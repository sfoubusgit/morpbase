/**
 * v3 Scenery lane — the "what happens" of the image (the action/moment).
 * Content wiped to a clean slate (2026-06-27); defaults to be rebuilt.
 */
export type SceneryItem = { id: string; name: string; summary: string; phrases: string[]; tint: number };

export const SCENERY_ITEMS: SceneryItem[] = [];
