/**
 * v3 Objects lane — props and artifacts that recur across scenes.
 * Content wiped to a clean slate (2026-06-27); defaults to be rebuilt.
 */
export type ObjectItem = { id: string; name: string; summary: string; phrases: string[]; tint: number };

export const OBJECT_ITEMS: ObjectItem[] = [];
