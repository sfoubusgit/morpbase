# MorpBase V2 Slice 2 Screen Map Prompt

Using the Slice 2 build brief, define the smallest real screen structure for `Community` as public circulation.

This slice must stay downstream from real kept work and must not become a separate content app.

## Decide

1. What are the minimum screens or screen-states needed?
2. Where does the user enter this slice from?
3. What is the strongest public view to start with?
4. What should still stay out of scope?

## Output

Write:

`MORPBASE_V2_SLICE_2_SCREEN_MAP.md`

Include:

- main screens or states
- transitions
- what the slice is really proving
