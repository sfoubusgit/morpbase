# MorpBase V2 After Continuity View Boundary Extraction Next Step Decision

Next move: Memory view boundary extraction planning

Why:
- the first support-realm extraction succeeded cleanly
- `Memory` is the next strongest candidate with manageable complexity
- `Community` is still more publicly branched and should wait one step longer

Why not `Workspace` yet:
- it is still the most sensitive center surface
- extracting it too early would carry more risk than value

Why not `Community` next:
- it still holds more branching public states, publishing actions, and participation logic
- `Memory` should be easier to split while keeping the pattern disciplined
