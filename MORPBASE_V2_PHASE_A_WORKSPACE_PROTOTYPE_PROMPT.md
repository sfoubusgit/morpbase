# MorpBase V2 Phase A Workspace Prototype Prompt

You are analyzing **MorpBase V2** as a **completely new rebuilt product in a new project folder**, not as a refinement of V1.

Your task is to define the **actual Phase A prototype brief** for the first prototype slice:

- the **core first-use Workspace slice**

This phase comes from:

- `MORPBASE_V2_PROTOTYPE_EXECUTION_WORKFLOW.md`

It is the first real prototype step and should produce a concrete artifact brief for the Workspace-first prototype.

## Core Context

The governing references are:

- `MORPBASE_V2_FOUNDATION_FREEZE.md`
- `MORPBASE_V2_PRODUCT_BLUEPRINT.md`
- `MORPBASE_V2_INTERACTION_BLUEPRINT.md`
- `MORPBASE_V2_TERMINOLOGY_MICROLANGUAGE_FREEZE.md`
- `MORPBASE_V2_DESIGN_UNIVERSE_PREPARATION.md`
- `MORPBASE_V2_PROTOTYPE_EXECUTION_WORKFLOW.md`

The Phase A slice must validate:

- direct entry into the real `Workspace`
- guided first-flow layer behavior
- authoring + `Prompt Preview` relationship
- first meaningful `Keep` / `Save` moment

## What This Brief Must Decide

You must define:

- what exact prototype artifacts should be produced for Phase A
- what the low-fidelity flow map should contain
- what the medium-fidelity prototype should contain
- what should be intentionally omitted from Phase A
- what success/failure should look like
- what Gate 1 review must evaluate

## Critical Constraints

- Do **not** expand into full-product prototyping
- Do **not** let `Memory`, `Community`, or `Continuity` dominate this phase
- Do **not** prototype all workspace complexity
- Do **not** turn this into a static page mockup; it must validate flow and behavior
- Do **not** drift into implementation planning

## Method

### 1. Start with real first-use validation moments

Write at least 5 moments inside the first-use Workspace slice, such as:

- arrival in the real Workspace
- seeing the guided first-flow layer
- making the first meaningful authoring choice
- trusting `Prompt Preview`
- deciding to keep/save

For each moment, explain what the prototype must prove.

### 2. Define the prototype artifact set

At minimum, define:

- low-fidelity flow map
- medium-fidelity interactive prototype
- optional higher-fidelity key-frame(s) if useful

### 3. Define the prototype scope

Be explicit about:

- what screens/states are in scope
- what actions must be testable
- what states can stay mocked or simplified

### 4. Define the gate

State exactly what Gate 1 should review before Phase B can begin.

## Output Requirements

Write the result as:

`MORPBASE_V2_PHASE_A_WORKSPACE_PROTOTYPE_BRIEF.md`

Structure it with:

1. Executive Conclusion
2. First-Use Validation Moments
3. Phase A Prototype Scope
4. Prototype Artifacts
5. Low-Fidelity Flow Map
6. Medium-Fidelity Prototype Requirements
7. Out-Of-Scope For Phase A
8. Gate 1 Review Criteria
9. Final Judgment

The result should be concrete enough that actual prototype work could begin from it immediately.
