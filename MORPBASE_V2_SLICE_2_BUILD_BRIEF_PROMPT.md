# MorpBase V2 Slice 2 Build Brief Prompt

Using the next-slice decision, define the second implementation slice for MorpBase V2.

This slice should be:

- `Community` as public circulation
- built on top of real kept work
- clearly downstream from the `Workspace + Memory` core loop

## Decide

1. What is the smallest correct `Community` slice?
2. What must it include so it feels real?
3. What must stay out so it does not become a separate content app?
4. What should the main loop of this slice be?

## Output

Write:

`MORPBASE_V2_SLICE_2_BUILD_BRIEF.md`

Include:

- Goal
- Include
- Exclude
- Main loop
- First checkpoint question
