# MorpBase V2 App-Level Smoke Boundary Checkpoint Prompt

Goal:
- decide whether the current app-level smoke layer is already the right size for the present V2 baseline

Current app-level smoke coverage:
- `Workspace -> Keep -> Memory`
- `Workspace -> Keep -> Memory -> Continue this line -> Workspace`

What to judge:
- whether the strongest private loop is now protected enough
- whether one more smoke path would materially reduce real risk
- whether broader UI smoke growth would now create more maintenance than value

Guardrails:
- stay proportional
- avoid coverage growth by momentum
- prefer protecting real product meaning over chasing surface count

Success condition:
- the smoke boundary is deliberately chosen
- the next move after this checkpoint is clear and disciplined
