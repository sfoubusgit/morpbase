# MorpBase V2 Focus And Path Wrappers Extraction Prompt

Extract the repeated focus-support and path-support wrappers out of the main app file.

This step should move:

- repeated `focus-prompt` wrappers
- repeated `link-cluster` wrappers with `path-chip-row`

Do not change product behavior.

Do not redesign the UI.

Do not add product features.

The goal is to reduce direct repeated UI structure inside the main app file while keeping the same live product reading.
