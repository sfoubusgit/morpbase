# MorpBase V2 Shared Render Fragments Extraction Prompt

Extract the small reusable presentational fragments out of the main app file.

This step should move:

- impact cluster rendering
- creator strip rendering
- invitation note rendering

Do not change product behavior.

Do not redesign the UI.

Do not add product features.

The goal is to make the current V2 baseline easier to protect by reducing direct UI concentration inside the main app file.
