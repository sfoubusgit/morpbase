# MorpBase V2 Minimal Core-Loop Smoke Pass Prompt

Goal:
- add the first very small app-level smoke coverage to the current V2 baseline

Target path:
- `Workspace -> Keep -> Memory`

What to prove:
- the app renders the real center
- a line can be shaped enough to cross the `Keep` threshold
- the `Keep` action moves the line into `Memory`
- the kept line becomes visible as a living return point

Guardrails:
- keep this to one meaningful path
- avoid broad UI coverage
- avoid browser automation
- avoid visual-detail assertions

Success condition:
- the first real app-level crossing is protected
- the pure-module safety net is now anchored to one real product path
