# MorpBase V2 To V1 Next Step Decision

## Decision
Stop treating current V2 as the main live direction.

Use:
- V1 as the active product base
- V2 as the conceptual extraction layer

## Immediate next step
Run a `V1 terminology and realm mapping audit`.

Purpose:
- find where `Workspace`, `Memory`, `Community`, `Continuity`, and `Profile` can map onto the current V1 product
- identify the smallest first rename/reframe pass that improves clarity without damaging V1’s stronger feel

## Why this is the right next step
- it keeps the value from V2
- it does not force the current V2 surface any further
- it respects the stronger user reaction to V1
- it creates a clean bridge back into real implementation
