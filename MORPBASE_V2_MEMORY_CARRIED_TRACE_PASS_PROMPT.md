# MorpBase V2 Memory Carried-Trace Pass Prompt

## Purpose

Apply the trace / carried-mark family to `Memory` as the first coded bridge.

The goal is to make `Memory` say more clearly:

- this line left readable value behind

without turning the second home into a motif-heavy surface.

## Important Constraint

`Memory` should not feel like:

- a decorative archive
- a map of connections
- or a symbolic showcase

It should feel like:

- a durable place where meaningful work left a readable trace

## Required Output

1. one brief for the pass
2. restrained carried-trace language on the main kept-work focus surface
3. a short review deciding whether the trace family feels stronger and still calm
