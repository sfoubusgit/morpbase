# MorpBase V2 Slice 2 First Checkpoint Prompt

Review the first coded `Community` slice in the new `morpbase-v2` project.

Do not judge only on technical success. Judge it as product behavior.

Answer:

`Does Community already feel like the public life of real MorpBase work, or does it still feel too much like a separate content layer?`

## Evaluate

1. Does the slice still feel clearly downstream from `Memory`?
2. Does `Discover` avoid generic-feed energy?
3. Does the public result still feel like a MorpBase object?
4. Does the return path into `Memory` feel real?
5. What still weakens the slice?

## Output

Write:

`MORPBASE_V2_SLICE_2_FIRST_CHECKPOINT.md`

Include:

- verdict
- what already works
- what still needs tightening
- next move
